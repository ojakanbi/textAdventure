public class Sword extends Weapon {

    //constructor method returns nothing
    // called the super class 'weapons' that extends to the parent class Item

    public Sword(){
        super("Sword", "A Sword with a rust, somewhat dangerous than a dagger", 10,20);
    }
}
